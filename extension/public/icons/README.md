`glance_logo.svg` is the source brand asset copied from `assets/`.

Generated PNG icons live alongside it:
- `glance-16.png`
- `glance-48.png`
- `glance-128.png`

To regenerate them, run `npm run generate:icons` from `extension/`.
